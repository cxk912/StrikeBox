<UserControl x:Class="StrikeBox.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部标题 -->
        <TextBlock Grid.Row="0"
                   Text="设置"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <!-- 设置表单 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
            <StackPanel>
                <!-- 解释器配置区域 -->
                <Border Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="24"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <!-- 区域标题 -->
                        <TextBlock Text="解释器配置"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,16" />

                        <Separator Margin="0,0,0,20"
                                   Background="{ui:ThemeResource CardStrokeColorDefaultBrush}" />

                        <!-- Python -->
                        <TextBlock Text="Python"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10" />

                        <Separator Margin="0,0,0,14"
                                   Background="{ui:ThemeResource CardStrokeColorDefaultBrush}" />

                        <Grid Margin="0,0,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="解释器路径"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"
                                       FontSize="13" />
                            <ui:TextBox Grid.Column="1"
                                        Text="{Binding PythonPath, UpdateSourceTrigger=PropertyChanged}"
                                        PlaceholderText="选择 python.exe 路径..." />
                            <ui:Button Grid.Column="2"
                                       Content="浏览"
                                       Margin="8,0,0,0"
                                       Command="{Binding BrowsePythonCommand}" />
                        </Grid>

                        <!-- Java -->
                        <TextBlock Text="Java"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10" />

                        <Separator Margin="0,0,0,14"
                                   Background="{ui:ThemeResource CardStrokeColorDefaultBrush}" />

                        <!-- Java 8 -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Java 8"
                                       VerticalAlignment="Center"
                                       FontSize="13" />
                            <ui:TextBox Grid.Column="1"
                                        Text="{Binding Java8Path, UpdateSourceTrigger=PropertyChanged}"
                                        PlaceholderText="选择 java.exe 路径..." />
                            <ui:Button Grid.Column="2"
                                       Content="浏览"
                                       Margin="8,0,0,0"
                                       Command="{Binding BrowseJava8Command}" />
                        </Grid>

                        <!-- Java 11 -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Java 11"
                                       VerticalAlignment="Center"
                                       FontSize="13" />
                            <ui:TextBox Grid.Column="1"
                                        Text="{Binding Java11Path, UpdateSourceTrigger=PropertyChanged}"
                                        PlaceholderText="选择 java.exe 路径..." />
                            <ui:Button Grid.Column="2"
                                       Content="浏览"
                                       Margin="8,0,0,0"
                                       Command="{Binding BrowseJava11Command}" />
                        </Grid>

                        <!-- Java 17 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Java 17"
                                       VerticalAlignment="Center"
                                       FontSize="13" />
                            <ui:TextBox Grid.Column="1"
                                        Text="{Binding Java17Path, UpdateSourceTrigger=PropertyChanged}"
                                        PlaceholderText="选择 java.exe 路径..." />
                            <ui:Button Grid.Column="2"
                                       Content="浏览"
                                       Margin="8,0,0,0"
                                       Command="{Binding BrowseJava17Command}" />
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮栏 -->
        <Border Grid.Row="2"
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16,12">
            <ui:Button Content="保存设置"
                       Appearance="Primary"
                       Icon="{ui:SymbolIcon Save16}"
                       Command="{Binding SaveCommand}"
                       HorizontalAlignment="Right" />
        </Border>
    </Grid>

</UserControl>
